---
// Hero Section amb imatge de fons real i overlay
---

<section class="relative min-h-screen flex items-center justify-center overflow-hidden">
  <!-- Imatge de fons -->
  <div class="absolute inset-0 z-0">
    <img 
      src="/hero-bg.jpg" 
      alt="Equip eComm360 treballant" 
      class="w-full h-full object-cover"
    />
    <!-- Overlay fosc blau -->
    <div class="absolute inset-0 bg-gradient-to-br from-[#1B5585]/95 via-[#1B5585]/90 to-[#55C7DC]/85"></div>
  </div>

  <!-- Contingut -->
  <div class="relative z-10 container-section text-center text-white">
    <!-- Badge superior -->
    <div class="mb-8 inline-block">
      <span class="inline-flex items-center px-6 py-3 rounded-full text-sm font-semibold tracking-wide uppercase bg-white/10 backdrop-blur-sm border border-white/20">
        Desenvolupament eCommerce Professional
      </span>
    </div>

    <!-- Títol principal MOLT gran -->
    <h1 class="text-6xl md:text-7xl lg:text-8xl xl:text-9xl font-bold mb-8 leading-tight">
      Creem experiències<br />
      <span class="text-[#55C7DC]">eCommerce</span> úniques<br />
      i rentables
    </h1>

    <!-- Subtítol -->
    <p class="text-xl md:text-2xl lg:text-3xl mb-12 max-w-4xl mx-auto leading-relaxed font-light">
      Transformem la teva visió en una botiga online que <strong class="font-semibold">vén i fidelitza</strong>. 
      Com agència eCommerce especialitzada, desenvolupem plataformes que s'integren completament en els teus processos.
    </p>

    <!-- Botons CTA -->
    <div class="flex flex-col sm:flex-row gap-6 justify-center items-center mb-16">
      <a 
        href="#contacte" 
        class="group px-10 py-5 bg-[#55C7DC] text-[#1B5585] font-bold text-lg rounded-2xl transition-all duration-300 shadow-2xl hover:shadow-[#55C7DC]/50 hover:scale-105 hover:bg-white inline-flex items-center gap-3"
      >
        Demana una consulta
        <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
        </svg>
      </a>
      <a 
        href="#serveis" 
        class="px-10 py-5 bg-white/10 backdrop-blur-sm text-white font-bold text-lg rounded-2xl transition-all duration-300 border-2 border-white/30 hover:bg-white hover:text-[#1B5585] hover:scale-105 inline-flex items-center gap-3"
      >
        Descobreix els nostres serveis
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
        </svg>
      </a>
    </div>

    <!-- Estadístiques -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-4xl mx-auto">
      <div class="backdrop-blur-md bg-white/10 rounded-2xl p-8 border border-white/20">
        <div class="text-5xl md:text-6xl font-bold text-[#55C7DC] mb-2">50+</div>
        <div class="text-lg md:text-xl font-light">Projectes completats</div>
      </div>
      <div class="backdrop-blur-md bg-white/10 rounded-2xl p-8 border border-white/20">
        <div class="text-5xl md:text-6xl font-bold text-[#55C7DC] mb-2">10+</div>
        <div class="text-lg md:text-xl font-light">Anys d'experiència</div>
      </div>
      <div class="backdrop-blur-md bg-white/10 rounded-2xl p-8 border border-white/20">
        <div class="text-5xl md:text-6xl font-bold text-[#55C7DC] mb-2">5</div>
        <div class="text-lg md:text-xl font-light">Plataformes dominades</div>
      </div>
    </div>

    <!-- Scroll indicator -->
    <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
      <svg class="w-8 h-8 text-white/60" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
      </svg>
    </div>
  </div>
</section>
